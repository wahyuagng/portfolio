export enum ColumnFormatEnum {
    TEXT = 'text',
    DECIMAL = 'decimal',
    CURRENCY = 'currency',
    BOOLEAN = 'boolean',
    DATE = 'date',
    DATETIME = 'dateTime',
    IMAGE = 'image',
    HTML = 'html',
}
